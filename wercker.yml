box: python

build:
    steps:
        - virtualenv

build:
    steps:
        - pip-install

build:
    steps:
        - install-packages:
            packages: openssh-client

        - script:
                name: python unit test
                code: |
                  echo "This is before weget"
                  wget https://download-installer.cdn.mozilla.net/pub/firefox/releases/42.0/linux-i686/en-US/firefox-42.0.tar.bz2
                  echo "after wget"
                  ttar -jxvf firefox-15.0.tar.bz2 -C /opt
                  echo "Tar compleated"
                  python manage.py test                   
